# https://goframe.org/docs/web/server-config-file-template
server:
  address:     ":18080"
  openapiPath: "/api.json"
  swaggerPath: "/swagger"

# https://goframe.org/docs/core/glog-config
logger:
  level:  "all"
  stdout: true

game:
  scoreStoragePath: "storage/scores.json"
  leaderboardLimit: 8
